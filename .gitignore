.claude
files.zip
.nojekyll
.DS_Store

# dependencias do lint (instaladas via npm install)
node_modules/

# gerados localmente, nao versionar
petermann-locaweb.zip
consultas-uso-storage.sql

# copia de trabalho fora do repo: backup do build 85, capturas de tela e
# rascunhos. Fica no disco, so nao e versionada.
SISTEMA DE NOTAS/
